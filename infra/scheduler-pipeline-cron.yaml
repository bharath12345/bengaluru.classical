# Cloud Scheduler: daily pipeline (apply via gcloud after Job exists)
# gcloud scheduler jobs create http pipeline-daily \
#   --location=asia-south1 \
#   --schedule="0 3 * * *" \
#   --time-zone="Asia/Kolkata" \
#   --uri="https://asia-south1-run.googleapis.com/apis/run.googleapis.com/v1/namespaces/bengaluru-classical/jobs/bengaluru-classical-pipeline:run" \
#   --http-method=POST \
#   --oauth-service-account-email=...

name: pipeline-daily
schedule: "0 3 * * *"
timeZone: Asia/Kolkata
description: Daily scrape + process_raw_ingests
